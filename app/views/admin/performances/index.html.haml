%h1 Performances

= will_paginate(@performances)

%table.table.table-striped
  %tr
    %th ID
    %th Composition
    %th Type
    %th URL
    %th Performer
    %th
    %th
    %th

  - @performances.each do |performance|
    %tr
      %td= performance.id
      %td= performance.composition.title
      %td= performance.type_name
      %td= link_to '<i class="icon-music">&nbsp;</i>'.html_safe,  performance.url.http
      %td= performance.person.canonical_name
      %td= link_to 'Show', admin_performance_path(performance)
      %td= link_to 'Edit', edit_admin_performance_path(performance)
      %td= link_to 'Destroy', admin_performance_path(performance), :confirm => 'Are you sure?', :method => :delete

%br

= will_paginate(@performances)

= link_to 'New Performance', new_admin_performance_path
